name: TMD Scraper

on:
  schedule:
    - cron: "0 13 * * *"   # 20:00 ไทย (13:00 UTC)
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      TZ: Asia/Bangkok
      CSV_OUT: tmd_7day_forecast_today.csv

    steps:
      # 1) Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2) Chrome สำหรับ Selenium
      - name: Setup Google Chrome
        uses: browser-actions/setup-chrome@v1

      # 3) Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 4) Dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas selenium webdriver-manager
          pip install google-api-python-client google-auth google-auth-httplib2

      # 5) Run scraper
      - name: Run scraper
        run: python scrap1.py

      # 6) Debug ไฟล์ CSV
      - name: List files
        run: ls -lh
      - name: Show CSV head
        run: |
          test -f "${{ env.CSV_OUT }}" || (echo "CSV not found!" && exit 1)
          head -n 20 "${{ env.CSV_OUT }}"

      # 7) เขียนไฟล์ service account จาก secret
      - name: Write SA JSON
        run: |
          echo '${{ secrets.SERVICE_ID }}' > sa.json

      # 8) Append + merge แล้วอัปเดตไฟล์เดิมบน Drive (รักษา DateTime เดิม)
      - name: Append-merge CSV into existing Drive file
        env:
          TMD_FILE_ID: ${{ secrets.TMD_FILE_ID }}
          CSV_OUT: ${{ env.CSV_OUT }}
        run: |
          python - <<'PY'
          import os, sys, io
          import pandas as pd
          from google.oauth2 import service_account
          from googleapiclient.discovery import build
          from googleapiclient.http import MediaFileUpload, MediaIoBaseDownload
          from googleapiclient.errors import HttpError

          file_id = os.environ.get("TMD_FILE_ID", "").strip()
          csv_path = os.environ.get("CSV_OUT", "tmd_7day_forecast_today.csv")

          if not file_id:
            print("❌ TMD_FILE_ID is empty or missing."); sys.exit(2)
          if not os.path.exists(csv_path):
            print(f"❌ CSV not found: {csv_path}"); sys.exit(2)

          SCOPES = ["https://www.googleapis.com/auth/drive"]
          creds = service_account.Credentials.from_service_account_file("sa.json", scopes=SCOPES)
          drive = build("drive", "v3", credentials=creds, cache_discovery=False)

          # 1) ตรวจการเข้าถึงไฟล์ปลายทาง
          try:
            meta = drive.files().get(fileId=file_id, fields="id,name,mimeType,owners(emailAddress)").execute()
            print(f"✅ Access OK: {meta['name']} ({meta['id']}) | owner={meta['owners'][0]['emailAddress']}")
            if meta["mimeType"].startswith("application/vnd.google-apps"):
              print("❌ Target is a Google Doc/Sheet. Only CSV files are supported.", file=sys.stderr)
              sys.exit(3)
          except HttpError as e:
            print(f"❌ Cannot access fileId={file_id}. Detail: {e}"); sys.exit(3)

          # 2) โหลดไฟล์เดิม
          old_df = pd.DataFrame()
          try:
            buf = io.BytesIO()
            req = drive.files().get_media(fileId=file_id)
            dl = MediaIoBaseDownload(buf, req)
            done = False
            while not done:
              status, done = dl.next_chunk()
            buf.seek(0)
            try:
              old_df = pd.read_csv(buf)
            except Exception:
              buf.seek(0)
              old_df = pd.read_csv(buf, encoding="utf-8-sig")
            print(f"ℹ️ Loaded existing rows: {len(old_df)}")
          except HttpError as e:
            print(f"⚠️ Could not download existing CSV, assume empty. Detail: {e}")

          # 3) โหลดข้อมูลใหม่
          try:
            new_df = pd.read_csv(csv_path)
            print(f"ℹ️ New rows: {len(new_df)}")
          except Exception as e:
            print(f"❌ Failed to read local CSV: {e}"); sys.exit(2)

          # 4) รวม + ลบซ้ำ (รักษา DateTime เป็น string)
          merged = pd.concat([old_df, new_df], ignore_index=True)

          if "DateTime" in merged.columns:
            merged["DateTime"] = merged["DateTime"].astype(str)

          if {"Province", "DateTime"}.issubset(merged.columns):
            merged.drop_duplicates(subset=["Province", "DateTime"], keep="last", inplace=True)
            # จัดเรียงด้วยคอลัมน์ช่วย (ไม่แตะค่า DateTime เดิม)
            sort_key = pd.to_datetime(merged["DateTime"], errors="coerce")
            merged = merged.assign(__sort_dt=sort_key).sort_values(["Province","__sort_dt"]).drop(columns="__sort_dt")
          else:
            merged.drop_duplicates(keep="last", inplace=True)

          tmp_path = "__merged_out.csv"
          merged.to_csv(tmp_path, index=False, encoding="utf-8-sig")
          print(f"📝 Merged rows total: {len(merged)}")

          # 5) อัปเดตทับไฟล์เดิม
          try:
            media = MediaFileUpload(tmp_path, mimetype="text/csv", resumable=False)
            updated = drive.files().update(
              fileId=file_id,
              media_body=media,
              fields="id,name,modifiedTime,size"
            ).execute()
            print(f"✅ Updated (appended): {updated['name']} | size={updated.get('size')} | modifiedTime={updated['modifiedTime']}")
          except HttpError as e:
            print(f"❌ Update failed: {e}"); sys.exit(4)
          PY
